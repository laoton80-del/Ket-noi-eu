/**
 * AF.SOS.1 — SOS Plus product surface (UI + copy only).
 * AF.SOS.2 — entitlement/consent/profile: see `sosPlusProduction.ts` + `domain/sos/*`.
 * No billing, Twilio, or backend enforcement from these flags alone.
 */
export const SOS_PLUS_PRODUCT_SURFACE_UI_ENABLED = true;
