/**
 * B2B broker economics — **net revenue share** (not merchant GMV).
 *
 * Canonical logic is implemented in `../api/BrokerService` (shared with webhooks & AI billing).
 * Import from this module when following the B2B service layout (`src/services/b2b/`).
 */
export * from '../api/BrokerService';
