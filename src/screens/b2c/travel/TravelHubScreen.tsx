import { TravelScreen } from '../TravelScreen';

/**
 * Stack route `TravelHub` — VIONA Travel shell (hybrid light + minimal premium accents).
 * UI lives in `TravelScreen.tsx` for CEO-mandated design system separation.
 */
export function TravelHubScreen() {
  return <TravelScreen />;
}
