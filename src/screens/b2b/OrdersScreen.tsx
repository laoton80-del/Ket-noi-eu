import type { ReactElement } from 'react';

import { B2BBookingDashboard } from './B2BBookingDashboard';

/** B2B Orders tab — Cyber-Functional salon booking command surface. */
export function OrdersScreen(): ReactElement {
  return <B2BBookingDashboard />;
}
