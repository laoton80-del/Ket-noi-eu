/**
 * Wallet / ví & Fintech entry (stack route `Wallet`).
 * UI implementation lives in `WalletTopUpScreen.tsx` — this file is the product-facing module name.
 */
export { WalletTopUpScreen as WalletScreen } from './WalletTopUpScreen';
