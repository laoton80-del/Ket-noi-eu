/**
 * TypeScript default resolution for `./nativeAppCheckBridge` (web stub).
 * Metro uses `.web` / `.ios` / `.android` ahead of this file at bundle time.
 */
export * from './nativeAppCheckBridge.web';
